import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    image:{
        marginLeft: 12,
        marginRight: 12,
        resizeMode: 'cover',
        marginBottom: 24,
    }
})
